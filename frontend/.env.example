# AskMiao 前端設定範本（3.0.0）
# 不建立 frontend/.env 時：前端呼叫相對路徑 /api，由 Vite 開發伺服器（埠號 3000）代理到 http://127.0.0.1:8001，不需設定 CORS
# 本範本採用「直接呼叫後端」模式：瀏覽器直接連到 VITE_API_BASE，後端 ALLOWED_ORIGINS 必須包含前端來源（預設已含 http://localhost:3001）
# 每個設定的說明見 docs/configuration.md

# 後端 API 位址（路徑結尾會自動補上 /api）；同時也是 Vite /api 代理的目標
VITE_API_BASE=http://localhost:8001
# VITE_API_BASE 未設定時才使用，格式相同
VITE_API_URL=http://localhost:8001
# Vite 開發伺服器埠號（未設定時為 3000）
PORT=3001
# 建置時是否輸出 source map，設為 false 才關閉
GENERATE_SOURCEMAP=true
